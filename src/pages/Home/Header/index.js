import { HeaderTag, HeaderContainer, HeaderTop } from "../index";

const Header = () => {
  return (
    <HeaderTag>
      <HeaderTop />
      <HeaderContainer />
    </HeaderTag>
  );
};

export default Header;
